# Feature Documentation

One file per shipped feature, named `<feature-name>.md`, created/updated when the
feature lands (per `CLAUDE.md` → Documentation Rules).

Each file contains: Purpose · Architecture · Data Flow · API/CLI surface ·
Schema/graph changes · Future Improvements.

_(empty until Phase 1 ships)_
